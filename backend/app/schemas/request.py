# Request schemas for the ForgeLens API.
#
# Image uploads are handled directly by FastAPI's UploadFile dependency,
# so no Pydantic request body model is required for the /analyse endpoint.
# This module is kept as a placeholder for future request schemas (e.g.
# batch analysis options, webhook configuration, etc.).
